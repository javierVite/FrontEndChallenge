// import {   useGetCommentsQuery,
//   useAddCommentMutation,
//   useUpdateCommentMutation,  
// } from './commentsApi';

const CommentsList = () => {
  // Use the hooks commented out above. You will not need to add/modify any code
  // besides what is in this file.

  // Create a function to toggle the approval status of a comment
  // Update the approval status

  // Handle loading and error states

  return (
    <div>
      <h2>Comments</h2>
      <ul>
        {/* Display the list of comments here */}
        {/* Each comment should have a button to toggle its approval status */}
      </ul>
    </div>
  );
};

export default CommentsList;
